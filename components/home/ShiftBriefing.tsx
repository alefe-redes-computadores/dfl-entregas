'use client';
import { useEffect,useMemo,useState } from 'react';
import { Bike,PackageCheck,Play,Store } from 'lucide-react';
import { useAppStore } from '@/store/useAppStore';
import { dateKey } from '@/lib/operational-time';
import { supplyDate } from '@/lib/stock-supply';

export function ShiftBriefing(){const settings=useAppStore(s=>s.storeSettings);const update=useAppStore(s=>s.updateStoreSettings);const motoboys=useAppStore(s=>s.motoboys.filter(item=>item.active));const supplies=useAppStore(s=>s.stockSupplies);const today=dateKey(new Date());const storageKey=`dfl-shift-started:${today}`;const [seen,setSeen]=useState(true);useEffect(()=>setSeen(localStorage.getItem(storageKey)==='1'),[storageKey]);const bought=useMemo(()=>supplies.filter(item=>dateKey(supplyDate(item))===today).length,[supplies,today]);if(seen)return null;const start=async()=>{await update({isOpen:true});localStorage.setItem(storageKey,'1');setSeen(true)};return <section className="rounded-[26px] border border-emerald-500/25 bg-gradient-to-br from-emerald-500/[.10] to-sky-500/[.05] p-4"><div className="flex items-center gap-3"><span className="grid h-11 w-11 place-items-center rounded-2xl bg-emerald-500/15 text-emerald-400"><Store size={20}/></span><div><p className="text-[10px] font-black uppercase tracking-[.16em] text-emerald-400">Primeira abertura do dia</p><h2 className="font-heading text-lg font-black text-zinc-100">Começar expediente?</h2></div></div><div className="mt-4 grid grid-cols-2 gap-2"><div className="rounded-2xl bg-zinc-950/45 p-3"><p className="flex items-center gap-1 text-[9px] font-black text-sky-400"><Bike size={11}/> EQUIPE ATIVA</p><p className="mt-1 text-xs font-bold text-zinc-300">{motoboys.length?motoboys.map(item=>item.name).join(', '):'Nenhum motoboy ativo'}</p></div><div className="rounded-2xl bg-zinc-950/45 p-3"><p className="flex items-center gap-1 text-[9px] font-black text-amber-400"><PackageCheck size={11}/> COMPRAS HOJE</p><p className="mt-1 text-xs font-bold text-zinc-300">{bought} registro{bought===1?'':'s'}</p></div></div><button onClick={start} className="mt-3 flex h-12 w-full items-center justify-center gap-2 rounded-2xl bg-emerald-500 text-sm font-black text-zinc-950"><Play size={16}/>Abrir operação de hoje</button>{settings.isOpen&&<p className="mt-2 text-center text-[10px] text-zinc-500">A loja já está marcada como aberta; confirme para dispensar este resumo.</p>}</section>}
