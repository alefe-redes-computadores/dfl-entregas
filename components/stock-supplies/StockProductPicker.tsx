'use client';
import { useMemo,useState } from 'react';import { Boxes,Check,Search,X } from 'lucide-react';import type { StockProduct } from '@/types';
export function StockProductPicker({products,value,onChange}:{products:StockProduct[];value?:string;onChange:(value:string)=>void}){const [open,setOpen]=useState(false),[query,setQuery]=useState('');const selected=products.find(p=>p.id===value);const groups=useMemo(()=>Object.entries(products.filter(p=>p.name.toLocaleLowerCase('pt-BR').includes(query.toLocaleLowerCase('pt-BR'))).reduce<Record<string,StockProduct[]>>((a,p)=>{(a[p.category||'Sem categoria']||=[]).push(p);return a},{})).sort(([a],[b])=>a.localeCompare(b,'pt-BR')),[products,query]);return <><button type="button" onClick={()=>setOpen(true)} className="mt-1 flex h-14 w-full items-center gap-3 rounded-2xl border border-zinc-800 bg-zinc-900 px-4 text-left"><Boxes size={17} className="text-amber-400"/><span className={`flex-1 truncate text-sm font-bold ${selected?'text-zinc-200':'text-zinc-500'}`}>{selected?.name||'Selecionar produto'}</span></button>{open&&<div onMouseDown={e=>{if(e.target===e.currentTarget)setOpen(false)}} className="fixed inset-0 z-[105] flex items-end bg-black/80 p-3 backdrop-blur-sm"><div className="mx-auto max-h-[90vh] w-full max-w-md overflow-y-auto rounded-[28px] border border-zinc-800 bg-zinc-950 p-5"><div className="flex justify-between"><div><h3 className="font-heading text-lg font-black text-zinc-100">Selecionar produto</h3><p className="text-xs text-zinc-600">Organizados por categoria.</p></div><button type="button" onClick={()=>setOpen(false)} className="grid h-10 w-10 place-items-center rounded-full bg-zinc-900 text-zinc-400"><X size={18}/></button></div><div className="relative mt-4"><Search size={16} className="absolute left-4 top-1/2 -translate-y-1/2 text-zinc-600"/><input autoFocus value={query} onChange={e=>setQuery(e.target.value)} placeholder="Buscar produto" className="h-12 w-full rounded-xl border border-zinc-800 bg-zinc-900 pl-11 pr-3 text-sm text-zinc-100 outline-none"/></div><button type="button" onClick={()=>{onChange('');setOpen(false)}} className="mt-3 h-11 w-full rounded-xl border border-dashed border-zinc-700 text-xs font-bold text-zinc-500">Produto personalizado</button><div className="mt-3 space-y-4">{groups.map(([category,items])=><section key={category}><p className="mb-2 text-[9px] font-black uppercase tracking-wider text-zinc-600">{category}</p><div className="space-y-2">{items.map(p=><button type="button" key={p.id} onClick={()=>{onChange(p.id);setOpen(false)}} className="flex w-full items-center gap-3 rounded-xl border border-zinc-800 bg-zinc-900/55 p-3 text-left"><span className="flex-1 text-sm font-bold text-zinc-200">{p.name}</span>{value===p.id&&<Check size={16} className="text-emerald-400"/>}</button>)}</div></section>)}</div></div></div>}</>}
