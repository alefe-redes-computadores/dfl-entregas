// app/estoque/relatorios/page.tsx
'use client';
import { AlertTriangle,BarChart3,TrendingDown,Wallet } from 'lucide-react';import { PageHeader } from '@/components/layout/PageHeader';import { useAppStore } from '@/store/useAppStore';import { stockLevel,stockValue } from '@/lib/stock';
export default function StockReports(){const products=useAppStore(s=>s.stockProducts.filter(p=>p.active));const movements=useAppStore(s=>s.stockMovements);const now=Date.now();const recent=movements.filter(m=>now-new Date(m.occurred_at).getTime()<=30*86400000);const losses=recent.filter(m=>m.type==='perda');const exits=recent.filter(m=>m.type==='saida');const value=stockValue(products);const lossValue=losses.reduce((sum,m)=>sum+m.quantity*(m.unit_cost||products.find(p=>p.id===m.product_id)?.average_cost||0),0);const ranking=[...products].map(p=>({p,usage:exits.filter(m=>m.product_id===p.id).reduce((s,m)=>s+m.quantity,0),loss:losses.filter(m=>m.product_id===p.id).reduce((s,m)=>s+m.quantity,0)})).sort((a,b)=>b.usage-a.usage);const money=(n:number)=>n.toLocaleString('pt-BR',{style:'currency',currency:'BRL'});const low=products.filter(p=>stockLevel(p)!=='ok');return <div className="pb-28"><PageHeader title="Inteligência de estoque" subtitle="Leitura explicável dos últimos 30 dias" to="/estoque"/><section className="grid grid-cols-2 gap-3"><Card icon={Wallet} label="Valor em estoque" value={money(value)}/><Card icon={TrendingDown} label="Perdas estimadas" value={money(lossValue)} alert={lossValue>0}/><Card icon={BarChart3} label="Saídas registradas" value={String(exits.length)}/><Card icon={AlertTriangle} label="Reposição necessária" value={String(low.length)} alert={low.length>0}/></section><section className="mt-5 rounded-[24px] border border-zinc-800 bg-zinc-900/45 p-4"><p className="text-[10px] font-black uppercase text-emerald-400">Leitura automática</p><h2 className="mt-2 font-heading text-lg font-black text-zinc-100">{low.length?`${low.length} produto${low.length===1?'':'s'} precisa${low.length===1?'':'m'} de atenção`:'Estoque dentro dos limites cadastrados'}</h2><p className="mt-2 text-xs leading-relaxed text-zinc-500">{recent.length?`Base: ${recent.length} movimentações registradas nos últimos 30 dias. Os valores usam o custo médio atual de cada produto.`:'Ainda não há amostra suficiente de movimentações. Registre entradas, saídas, perdas e contagens para formar uma leitura confiável.'}</p></section><section className="mt-5"><h2 className="mb-3 font-heading font-black text-zinc-100">Consumo por produto</h2><div className="space-y-2">{ranking.filter(r=>r.usage||r.loss).map(({p,usage,loss})=><article key={p.id} className="rounded-2xl border border-zinc-800 bg-zinc-900/50 p-4"><div className="flex justify-between"><p className="font-black text-zinc-200">{p.name}</p><p className="text-xs font-black text-emerald-400">{usage.toLocaleString('pt-BR')} saíram</p></div>{loss>0&&<p className="mt-2 text-[10px] font-bold text-red-400">{loss.toLocaleString('pt-BR')} registrados como perda</p>}</article>)}{!ranking.some(r=>r.usage||r.loss)&&<p className="rounded-2xl border border-dashed border-zinc-800 py-10 text-center text-xs text-zinc-600">Sem consumo registrado nos últimos 30 dias.</p>}</div></section></div>}
function Card({icon:Icon,label,value,alert}:{icon:typeof Wallet;label:string;value:string;alert?:boolean}){return <div className={`min-w-0 rounded-[22px] border p-4 ${alert?'border-amber-500/25 bg-amber-500/[.05]':'border-zinc-800 bg-zinc-900/45'}`}><Icon size={16} className={alert?'text-amber-400':'text-emerald-400'}/><p className="mt-3 truncate font-heading text-xl font-black text-zinc-100">{value}</p><p className="mt-1 text-[10px] font-bold text-zinc-600">{label}</p></div>}
